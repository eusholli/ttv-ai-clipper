# Cloudflare R2 Storage Configuration
# Account ID from Cloudflare Dashboard
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
# API Token with R2 permissions
CLOUDFLARE_TOKEN_ID=your_cloudflare_api_token
# R2 Access Key ID from R2 settings
CLOUDFLARE_ACCESS_KEY_ID=your_r2_access_key_id
# R2 Secret Access Key from R2 settings
CLOUDFLARE_SECRET_ACCESS_KEY=your_r2_secret_access_key
# Name of your R2 bucket
CLOUDFLARE_BUCKET_NAME=your_bucket_name

# Stripe Payment Configuration
# Secret key from Stripe Dashboard (test or live mode)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# Webhook signing secret for verifying Stripe webhook events
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Database Configuration
# Database name (default: transcript_search)
DB_NAME=transcript_search
# Database user (default: transcript_user)
DB_USER=transcript_user
# Database password (default: transcript_pwd)
DB_PWD=transcript_pwd
# Database host settings:
# - Local development: DB_HOST=localhost
# - VSCode devcontainer: DB_HOST=host.docker.internal (to access host machine's PostgreSQL)
# - Production: Not needed (uses container's PostgreSQL)
DB_HOST=host.docker.internal

# Email Notification Configuration
# SMTP server hostname (e.g., smtp.gmail.com)
SMTP_SERVER=smtp.example.com
# SMTP server port (usually 587 for TLS)
SMTP_PORT=587
# SMTP username (usually your email address)
SMTP_USERNAME=your_email@example.com
# SMTP password (for Gmail, use App Password)
SMTP_PASSWORD=your_smtp_password
# From email address for notifications
FROM_EMAIL=noreply@example.com

# Clerk Authentication Configuration
# Public key for verifying Clerk JWTs (from Clerk Dashboard -> JWT Templates)
CLERK_JWT_KEY=your_clerk_jwt_public_key
